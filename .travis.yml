language: java

jdk: oraclejdk8

addons:
  apt:
    packages: oracle-java8-installer

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.gradle/native/
    - $HOME/.gradle/daemon/native/
